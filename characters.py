
class Characters:
    def __init__(self, name, nation, banding_element, current_hp, max_hp, current_mana, max_mana, current_weapon,
                 weapons, balance):
        self.name = name
        self.nation = nation
        self.banding_element = banding_element
        self.current_ho = current_hp
        self.max_hp = max_hp
        self.current_mana = current_mana
        self.max_mana = max_mana
        self.current_weapon = current_weapon
        self.weapons = weapons
        self.balance = balance
        #print(f'{self.name} erstellt.')

